<div class="form-default">

  <div class="title">
    <h2>Olá, aqui você pode alterar seus dados</h2>
  </div>

  <div class="subtitle">
    <h3>Por favor, preencha seus dados</h3>
  </div>

  <hr/>


  <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
    <%= devise_error_messages! %>

    <label>Deseja alterar seu email? Informe seu novo email</label>
    <div class="field">
      <%= f.email_field :email, autofocus: true %>
    </div>

    <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
      <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
    <% end %>

    <label>Deseja alterar sua senha atual? Informe sua nova senha, caso contrário, deixa este campo em branco</label>
    <div class="field">
      <%= f.password_field :password, autocomplete: "off" %>
    </div>

    <label>Confirme sua nova senha</label>
    <div class="field">
      <%= f.password_field :password_confirmation, autocomplete: "off" %>
    </div>

    <label>Por favor, informe sua senha atual, ela é necessária para salvar as alterações em sua conta</label>
    <div class="field">
      <%= f.password_field :current_password, autocomplete: "off" %>
    </div>

    <div class="actions">
      <%= f.submit "Atualizar informações" %>
    </div>
  <% end %>

  <!--
  <h3>Cancel my account</h3>

  <p>Unhappy? <%= button_to "Cancel my account", registration_path(resource_name), data: { confirm: "Are you sure?" }, method: :delete %></p>

  <%= link_to "Back", :back %>
  -->
</div>